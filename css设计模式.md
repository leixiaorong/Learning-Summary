```js
负责显示数据的组件不要固定外部样式，而是固定内部样式


页面结构类的组件：头部、导航、侧边栏、banner区（广告位、主内容区、底部

复用率很高的组件（有自己专属的js和css，可直接嵌入到页面使用：分页、弹窗、面包屑

css层级不要太多，最多不超过4层，因为是从右向左解析

群组选择器（此方式不够优雅，优雅的做法是将公共样式定义成公共类，然后在每次用的时候放在元素里
https://blog.csdn.net/ixygj197875/article/details/78692526

css和预处理器
sass和less提供了很多便利：定义统一的变量，使用嵌套而不是重复的选择器

oocss 面向对象的css设计
base 基础的样式，一些事先定义好的，针对某类元素的通用样式
layout 布局 与整体结构相关、侧边栏的位置、主内容、宽高
module 模块 抽取出来的部分，这类的样式
state 状态 页面上的元素显示不同的状态 可以 不可用 
theme 整个版面的风格


```